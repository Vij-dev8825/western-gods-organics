// Single source of truth for the canonical production origin — used for
// the per-route <link rel="canonical"> tag (App.jsx) and for building
// absolute URLs in structured data / any other place that needs one.
export const CANONICAL_ORIGIN = 'https://www.westerngodsorganic.com';
